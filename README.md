# blog-issues

本仓库用于通过 GitHub Issues 发布博客内容。

## 已配置标签

- `blog`（`#3b82f6`）：标记文章
- `project`（`#22d3ee`）：标记项目

可按需新增自定义标签（如 `技术` / `随笔` / `项目`）用于筛选。

## 发布流程

1. 打开 **Issues**，点击 **New issue**。
2. 标题填写文章标题。
3. 正文使用 Markdown 编写内容。
4. 选择对应标签（`blog` 或 `project`）。
5. 发布 Issue，前端自动同步。
